{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

<div class="container text-white w-25">
    <h1>Votre spé</h1>

    {{ form_errors(registrationFullForm) }}

    {{ form_start(registrationFullForm) }}
        {{ form_row(registrationFullForm.classe) }}
        {{ form_row(registrationFullForm.spe) }}

        <button type="submit" class="btn btn-primary mt-2">Envoyer</button>
        <button type="cancel" class="btn btn-danger mt-2">Annuler</button>
    {{ form_end(registrationFullForm) }}
    </div>
{% endblock %}